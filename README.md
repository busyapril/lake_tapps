# FinalSolutionGannt

Gantt Chart Application with Vue.js and Node.js.

## Deployment

Deployed to GitHub Pages at: https://zhendrix3019.github.io/FinalSolutionGannt/

# Lake Tapps Gantt Chart Application

A powerful, interactive Gantt Chart application built with **Vue 3 (Vite)** on the frontend and **Node.js (Express)** with **Sequelize (SQLite)** on the backend.

## Features

- **Interactive Gantt Chart**: Drag-and-drop scheduling, resizing, and moving of tasks.
- **Multiple Row Types**: Supports Tasks, Milestones, Sections, and Text lines.
- **Scale Presets**: View by Days, Weeks, or Months.
- **Dark Mode**: Fully integrated dark mode support.
- **Authentication**: Google OAuth and local development login.
- **Database**: SQLite database managed via Sequelize ORM.

## Getting Started

### Prerequisites

- Node.js (v16 or higher)
- npm or pnpm

### Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/busyapril/lake_tapps.git
   cd lake_tapps
   ```

2. Install dependencies for both frontend and backend:
   ```bash
   # Install backend dependencies
   cd backend
   npm install

   # Install frontend dependencies
   cd ../frontend
   npm install
   ```

### Running the Application

You can start the application using the provided batch script (Windows):
- Double-click `start-clean.bat` in the root directory.

Or run them manually:

1. **Start the Backend**:
   ```bash
   cd backend
   npm start
   ```
   The backend will run on `http://localhost:3001`.

2. **Start the Frontend**:
   ```bash
   cd frontend
   npm run dev
   ```
   The frontend will run on `http://localhost:3000`.

## License

MIT

# Lake Tapps Gantt Chart Application

# Lake Tapps Gantt Chart Application
